# torch / torchvision ставятся ОТДЕЛЬНО с CUDA-индекса (см. инструкцию, шаг 2),
# поэтому их тут нет — иначе pip может поставить CPU-сборку.
ultralytics~=8.4.63
opencv-python~=4.13.0.92
numpy~=2.2.6
tensorrt-cu12